# LabUI — consolidated runtime dependencies (tested on Python 3.12, Apple Silicon).
# Single source of truth for the whole app (Research RAG + Journal-Fit tracker).
#
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt
#   pip install -e submission_strategy      # installs the `jfr` package + CLI
#
# Versions are floors at the last known-good set; bump deliberately.

# ── Web server ───────────────────────────────────────────────────────────────
fastapi>=0.137
uvicorn[standard]>=0.49
jinja2>=3.1
python-multipart>=0.0.32          # file uploads (papers)
aiofiles>=25
pydantic>=2.13
pydantic-settings>=2.14
httpx>=0.28
requests>=2.34

# ── Retrieval / embeddings / rerank ──────────────────────────────────────────
sentence-transformers>=5.5         # bge-m3 embedder + bge reranker
transformers>=4.57
torch>=2.12
sqlite-vec>=0.1.9                  # vector search SQLite extension
rank-bm25>=0.2.2
numpy>=2.0

# ── PDF ingestion ────────────────────────────────────────────────────────────
docling>=2.102                     # primary PDF → structured-text parser
pymupdf>=1.24                      # fitz: fast text extraction for chat attachments

# ── Generation ───────────────────────────────────────────────────────────────
# The generator runs in Ollama (external service) — no Python package needed.
# Install Ollama separately and `ollama pull <a-model>` (auto-detected at runtime).

# ── Web search augmentation ──────────────────────────────────────────────────
ddgs>=9.14                         # DuckDuckGo (multi-backend) web results

# ── Journal-Fit tracker (JFR) ────────────────────────────────────────────────
apscheduler>=3.11
feedparser>=6                      # journal RSS corpus refresh
pyyaml>=6
qdrant-client>=1.18
peft>=0.19
typer>=0.24
rich>=14
tqdm>=4.68

# ── Optional: knowledge-graph extraction (the /research/graph features) ───────
# Heavy (~spaCy model + GLiNER). Install only if you use the entity graph:
#   pip install "gliner>=0.2.27" "spacy>=3.8" && python -m spacy download en_core_web_sm
# gliner>=0.2.27
# spacy>=3.8

# ── Optional: retrieval evaluation (ragas) — not needed to run the app ────────
# ragas>=0.4
# datasets
